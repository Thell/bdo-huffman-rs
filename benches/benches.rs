#![allow(unused)]

use common;

use baseline;

use nested_box;
use nested_unsafe_box;

use flat_index;
use flat_ptr;
use flat_unsafe_ptr;

use table_index;
use table_ptr;
use table_unsafe_ptr;

fn main() {
    divan::main();
}
